---
name: typescript
description: TypeScript conventions — type design, inference-first, avoiding over-typing, assertion rules
---

# TypeScript approach

## Inference first

<!-- Prefer inferred return types, let the compiler work, when explicit annotations add value -->

## Type design

<!-- Composing types from primitives, discriminated unions, avoiding wrapper types -->

## Assertions & casts

<!-- `const x: T = {}` over `as T`, when `as` is acceptable (function return sites), never as a substitute for proper typing -->

## Generics

<!-- When generics earn their complexity, avoiding premature abstraction -->

## Common pitfalls

<!-- Things to avoid: enums, namespace, triple-slash references, etc. -->
